.page-header
  %h1.title Subjects

.row
  - @subjects.in_groups(3, false) do |group|
    .col-md-4
      %ol{start: 1+@subjects.index(group.first).to_i}
        = list_of(group) do |subject|
          = link_to(subject) do
            = subject.name

= content_for(:sidebar) do
  - if can?(:create, Subject)
    %li
      %h4
        Actions
      %ul.nav
        %li
          = link_to(new_subject_path, accesskey: "n") do
            = fa_icon("plus")
            New Subject
